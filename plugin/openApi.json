{
  "openapi": "3.0.3",
  "info": {
    "title": "AJRM Marine Display API",
    "version": "0.6.20",
    "description": "Display status, chart-webapp projections and display-only Signal K v2 route management backed by AJRM Marine Traffic and Notifications Plus."
  },
  "servers": [
    {
      "url": "/plugins/signalk-ajrm-marine-display"
    }
  ],
  "paths": {
    "/status": {
      "get": {
        "summary": "Get Display runtime status and browser defaults",
        "responses": {
          "200": {
            "description": "Current Display plugin state"
          }
        }
      }
    },
    "/getTargets": {
      "get": {
        "summary": "Get AJRM Marine Traffic targets projected for the Display webapp",
        "responses": {
          "200": {
            "description": "Projected targets keyed by MMSI"
          }
        }
      }
    },
    "/uiState": {
      "get": {
        "summary": "Get Display UI state projected from AJRM Marine Traffic, Notifications Plus and Audio",
        "responses": {
          "200": {
            "description": "Current Display UI state"
          }
        }
      }
    },
    "/refreshDiagnostics": {
      "post": {
        "summary": "Report a browser slow-refresh diagnostic sample through Signal K plugin debug logging",
        "responses": {
          "200": {
            "description": "Diagnostic sample accepted"
          }
        }
      }
    },
    "/debugControls": {
      "get": {
        "summary": "Get transient browser rendering debug controls",
        "responses": {
          "200": {
            "description": "Current transient debug controls"
          }
        }
      },
      "post": {
        "summary": "Update transient browser rendering debug controls",
        "responses": {
          "200": {
            "description": "Debug controls updated"
          }
        }
      }
    },
    "/observations/status": {
      "get": {
        "summary": "Check whether an active AJRM Marine Capture voyage can accept observations",
        "responses": {
          "200": {
            "description": "Capture, voyage and optional diagnostic-snapshot availability"
          }
        }
      }
    },
    "/observations": {
      "post": {
        "summary": "Append a timestamped observation to the active voyage",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": [
                  "text",
                  "includeSnapshot"
                ],
                "properties": {
                  "text": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 2000
                  },
                  "includeSnapshot": {
                    "type": "boolean",
                    "description": "Request an AJRM Marine diagnostic snapshot at the same observation time."
                  }
                }
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Observation saved by AJRM Marine Capture"
          },
          "409": {
            "description": "No active voyage or observation could not be saved"
          },
          "503": {
            "description": "AJRM Marine Capture observation support is unavailable"
          }
        }
      }
    },
    "/routes": {
      "get": {
        "summary": "List the active route, Signal K v2 route resources and GPX files in the configured Pi directory",
        "responses": {
          "200": {
            "description": "Route manager state and available routes"
          }
        }
      }
    },
    "/routes/import": {
      "post": {
        "summary": "Import and display a browser-supplied GPX 1.1 route",
        "responses": {
          "200": {
            "description": "Imported route is active"
          },
          "409": {
            "description": "GPX route could not be imported"
          }
        }
      }
    },
    "/routes/open-pi": {
      "post": {
        "summary": "Open and display a GPX 1.1 route from the configured Pi directory",
        "responses": {
          "200": {
            "description": "Pi route is active"
          }
        }
      }
    },
    "/routes/open-resource": {
      "post": {
        "summary": "Open and display a Signal K v2 route resource",
        "responses": {
          "200": {
            "description": "Signal K route resource is active"
          }
        }
      }
    },
    "/routes/delete-resource": {
      "post": {
        "summary": "Delete a selected Signal K v2 route resource",
        "description": "Deletes only the selected Signal K resource UUID. If it is the active displayed route, the route is also closed. Pi GPX files are not deleted.",
        "responses": {
          "200": {
            "description": "Signal K route resource was deleted"
          },
          "409": {
            "description": "Route was not found or could not be deleted"
          }
        }
      }
    },
    "/routes/reverse": {
      "post": {
        "summary": "Toggle the active route coordinate and waypoint order",
        "responses": {
          "200": {
            "description": "Reversed route is active"
          }
        }
      }
    },
    "/routes/save": {
      "post": {
        "summary": "Save or save-as the active route to Signal K and the configured Pi GPX directory",
        "responses": {
          "200": {
            "description": "Route was saved"
          }
        }
      }
    },
    "/routes/close": {
      "post": {
        "summary": "Close the displayed route without changing the Signal K Course API",
        "responses": {
          "200": {
            "description": "No route is active"
          }
        }
      }
    },
    "/routes/export": {
      "get": {
        "summary": "Download the active route as OpenCPN-compatible GPX 1.1",
        "responses": {
          "200": {
            "description": "GPX route document"
          },
          "409": {
            "description": "No route is active"
          }
        }
      }
    }
  }
}
