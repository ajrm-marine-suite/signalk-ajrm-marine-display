{
  "openapi": "3.0.3",
  "info": {
    "title": "AJRM Marine Display API",
    "version": "0.6.3",
    "description": "Display status and chart-webapp projections backed by AJRM Marine Traffic and Notifications Plus."
  },
  "servers": [
    {
      "url": "/plugins/signalk-ajrm-marine-display"
    }
  ],
  "paths": {
    "/status": {
      "get": {
        "summary": "Get Display runtime status and browser defaults",
        "responses": {
          "200": {
            "description": "Current Display plugin state"
          }
        }
      }
    },
    "/getTargets": {
      "get": {
        "summary": "Get AJRM Marine Traffic targets projected for the Display webapp",
        "responses": {
          "200": {
            "description": "Projected targets keyed by MMSI"
          }
        }
      }
    },
    "/uiState": {
      "get": {
        "summary": "Get Display UI state projected from AJRM Marine Traffic, Notifications Plus and Audio",
        "responses": {
          "200": {
            "description": "Current Display UI state"
          }
        }
      }
    },
    "/refreshDiagnostics": {
      "post": {
        "summary": "Report a browser slow-refresh diagnostic sample through Signal K plugin debug logging",
        "responses": {
          "200": {
            "description": "Diagnostic sample accepted"
          }
        }
      }
    },
    "/debugControls": {
      "get": {
        "summary": "Get transient browser rendering debug controls",
        "responses": {
          "200": {
            "description": "Current transient debug controls"
          }
        }
      },
      "post": {
        "summary": "Update transient browser rendering debug controls",
        "responses": {
          "200": {
            "description": "Debug controls updated"
          }
        }
      }
    },
    "/observations/status": {
      "get": {
        "summary": "Check whether an active AJRM Marine Capture voyage can accept observations",
        "responses": {
          "200": {
            "description": "Capture, voyage and optional diagnostic-snapshot availability"
          }
        }
      }
    },
    "/observations": {
      "post": {
        "summary": "Append a timestamped observation to the active voyage",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": [
                  "text",
                  "includeSnapshot"
                ],
                "properties": {
                  "text": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 2000
                  },
                  "includeSnapshot": {
                    "type": "boolean",
                    "description": "Request an AJRM Marine diagnostic snapshot at the same observation time."
                  }
                }
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Observation saved by AJRM Marine Capture"
          },
          "409": {
            "description": "No active voyage or observation could not be saved"
          },
          "503": {
            "description": "AJRM Marine Capture observation support is unavailable"
          }
        }
      }
    }
  }
}
