/**
 * Builds configuration for app display settings event binding in the AJRM Marine Display browser application.
 */

export const GPS_LOSS_CONTROL_IDS = {
	pauseButton: "buttonPauseGpsAlarm",
	alertModal: "modalAlert",
};
