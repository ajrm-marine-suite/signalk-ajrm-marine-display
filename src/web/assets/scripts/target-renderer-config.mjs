export function targetRendererCountElements(elements) {
	return {
		total: elements.totalTargetCountUI,
		filtered: elements.filteredTargetCountUI,
		alarm: elements.alarmTargetCountUI,
	};
}

export function targetRendererConfig({
	map,
	targets,
	boatMarkers,
	boatProjectedCourseLines,
	targetOverlays,
	labelCollision,
	rangeRings,
	autoCharts,
	harbourDisplay,
	autoProfileSettings,
	speechOutput,
	serverAlertEvents,
	targetSilence,
	targetSelection,
	selectionMarkers,
	modals,
	elements,
	getSelfMmsi,
	getSelfTarget,
	getCollisionProfiles,
	getSelectedVesselMmsi,
	setSelectedVesselMmsi,
	getSortTableBy,
	getMapFollowSelf,
	getDisableMapPanTo,
	getDisplayHarbours,
	setDisableMoveend,
	targetMaxAge,
	courseProjectionMinutes,
}) {
	return {
		map,
		targets,
		boatMarkers,
		boatProjectedCourseLines,
		targetOverlays,
		labelCollision,
		rangeRings,
		autoCharts,
		harbourDisplay,
		autoProfileSettings,
		speechOutput,
		serverAlertEvents,
		targetSilence,
		targetSelection,
		selectionMarkers,
		selectedVesselModal: modals.selectedVesselProperties,
		countElements: targetRendererCountElements(elements),
		getSelfMmsi,
		getSelfTarget,
		getCollisionProfiles,
		getSelectedVesselMmsi,
		setSelectedVesselMmsi,
		getSortTableBy,
		getMapFollowSelf,
		getDisableMapPanTo,
		getDisplayHarbours,
		setDisableMoveend,
		targetMaxAge,
		courseProjectionMinutes,
	};
}
