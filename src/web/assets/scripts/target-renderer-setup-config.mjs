import { targetRendererConfig } from "./target-ui-config.mjs";

export function configuredTargetRendererConfig({
	map,
	targets,
	boatMarkers,
	boatProjectedCourseLines,
	targetOverlays,
	labelCollision,
	rangeRings,
	autoCharts,
	harbourDisplay,
	autoProfileSettings,
	speechOutput,
	serverAlertEvents,
	targetSilence,
	targetSelection,
	selectionMarkers,
	modals,
	elements,
	getSelfMmsi,
	getSelfTarget,
	getCollisionProfiles,
	getSelectedVesselMmsi,
	setSelectedVesselMmsi,
	getSortTableBy,
	getMapFollowSelf,
	getDisableMapPanTo,
	getDisplayHarbours,
	setDisableMoveend,
	targetMaxAge,
	courseProjectionMinutes,
}) {
	return targetRendererConfig({
		map,
		targets,
		boatMarkers,
		boatProjectedCourseLines,
		targetOverlays,
		labelCollision,
		rangeRings,
		autoCharts,
		harbourDisplay,
		autoProfileSettings,
		speechOutput,
		serverAlertEvents,
		targetSilence,
		targetSelection,
		selectionMarkers,
		modals,
		elements,
		getSelfMmsi,
		getSelfTarget,
		getCollisionProfiles,
		getSelectedVesselMmsi,
		setSelectedVesselMmsi,
		getSortTableBy,
		getMapFollowSelf,
		getDisableMapPanTo,
		getDisplayHarbours,
		setDisableMoveend,
		targetMaxAge,
		courseProjectionMinutes,
	});
}
