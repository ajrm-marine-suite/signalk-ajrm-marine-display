/**
 * Defines API routes and access helpers for server alert events in the AJRM Marine Display browser application.
 */

export function alertEventsPath(_pluginId) {
	return `/signalk/v1/api/ajrmMarineDisplay/alertEvents`;
}
